const capitalize = require('./capitalize.js');
const makeCallbackResponse = require('./makeCallbackResponse.js');
const facade = require('./workoutDBFacade');
module.exports = {
    capitalize: capitalize,
    makeCallbackResponse: makeCallbackResponse,
    facade2: facade
}

